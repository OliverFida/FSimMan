<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="clr-namespace:OliverFida.FSimMan.UI">
    <Style x:Key="HeadingBaseStyle" TargetType="{x:Type ui:Heading}">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontSize" Value="{StaticResource SectionHeadingFontSize}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="FontFamily" Value="Arial" />
        <Setter Property="Margin" Value="0,10,0,5" />
    </Style>

    <Style BasedOn="{StaticResource HeadingBaseStyle}" TargetType="{x:Type ui:Heading}" />

    <Style
        x:Key="InputGroupHeadingStyle"
        BasedOn="{StaticResource HeadingBaseStyle}"
        TargetType="{x:Type ui:Heading}">
        <Setter Property="FontSize" Value="{StaticResource InputGroupFontSize}" />
        <Setter Property="Padding" Value="0" />
    </Style>
</ResourceDictionary>