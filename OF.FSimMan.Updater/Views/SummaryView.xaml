<UserControl
    x:Class="OF.FSimMan.Updater.Views.SummaryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cmp="clr-namespace:CLS.Core.Wpf.Component;assembly=CLS.Core.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:OF.FSimMan.Updater.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:OF.FSimMan.Updater.ViewModels"
    d:DataContext="{d:DesignInstance Type={x:Type vm:SummaryViewModel}}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <cmp:Heading
                Grid.Row="0"
                Text="FSimMan"
                TextAlignment="Center" />
            <TextBlock
                Grid.Row="1"
                Margin="0,5,0,0"
                Text="{Binding StatusText, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                TextAlignment="Center" />
            <cmp:Button
                Grid.Row="2"
                Margin="0,50,0,0"
                Command="{Binding InstallOrCancelCommand}"
                Content="{Binding InstallOrCancelButtonText, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>
    </Grid>
</UserControl>
